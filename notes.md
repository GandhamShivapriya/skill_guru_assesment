note:in production we use express-rate-limit but for this assessment we are using a simple in-memory rate limiter to demonstrate the concept.

🔑“Build a minimal backend API”
That single word “minimal” changes everything.
So we used:
In - memory array instead of DB
Plain JS objects instead of schemas